/src
This folder contains all Power Query M-code modules and defined functions used in the REFIT scoring system.

Each query is stored with semantic versioning:

v1.x for incremental improvements
v2.x for breaking changes
Queries are organized into subfolders by functional area.
