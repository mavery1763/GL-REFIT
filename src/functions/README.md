# /src/functions — Query Utility Functions

This folder will contain reusable Power Query functions such as:

- `GetSetting()` — centralized retrieval of Settings sheet parameters  
- Future helper functions (e.g., BD scoring engines, type coercion helpers)

Functions will be versioned and documented similarly to module code.

Modules may import shared functions from this directory.

